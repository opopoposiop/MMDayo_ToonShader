# Contributing

## 対象範囲

- ルートの `ToonAnime_32.fxdayo` が現行版です。
- `archive/` はローカル専用の過去版置き場です。Gitへ追加せず、新しい修正は原則として現行版へ行います。
- `ToonAnime.pmx` と `initial.vmd` のモーフ名・値域を変更する場合は、シェーダー側の controllers と同時に確認します。

## 変更時の確認

描画パス、深度、透過、GBuffer、SSAO、PPAL の受け渡しは相互に依存しています。次の項目を単独で変更しないでください。

- パス名、パス順、リソース名、リソース形式
- ブレンド設定、深度テスト、深度書き込み、カリング
- GBuffer のパッキングと PPAL 側のデコード契約
- 透明ピクセルの discard 条件と SSAO の遮蔽判定

処理方法はコードの構造で読めるようにし、コメントには採用しなかった方法や変更時の制約だけを残します。

過去版との比較や回帰の経緯は `CHANGELOG.md` に記録し、シェーダー本体へ詳細な変更履歴を追加しないでください。コミットログには、何を変更したかだけでなく、なぜ変更が必要だったかを記録してください。

## 動作確認

変更後は [`docs/validation.md`](docs/validation.md) の確認項目を実行してください。実際に確認していない GPU、MikuMikuDayo の版、PPAL の組み合わせを対応済みとは記載しないでください。

## Issue 報告

次の情報を添えて報告してください。

- ToonAnime のファイル名とコミットまたはリリース
- MikuMikuDayo の版またはコミット
- GPU とドライバー、DXR の利用可否
- 使用した PMX、VMD、PPAL の構成
- 透視投影／平行投影、透明・発光材質の条件
- 再現手順、期待結果、実際の結果、スクリーンショット
